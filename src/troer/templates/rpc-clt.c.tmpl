/******************************************************************************
 * SPDX-License-Identifier: MIT-0
 *
 * This file is part of ${name}.
 * This file is generated by troer, don't edit manually.
 ******************************************************************************/

#if $kconfig
\#include "${name}/lib.h"
#else if $args.include_prefix
\#include "${args.include_prefix}${name}.h"
#else
\#include "${name}.h"
#end if
#set $rpc = $elems['rpc-enum']
#for $e in $rpc.req

#if 'request' in $e
int
${e.pre}req_${e.id}(struct galv_rpc_clnt *client,
                    ${e.request.type} ${e.request.asterisk}req,
                    ${e.pre}clt_${e.id}_fn *cb,
                    void * ctx)
{
#if $e.request.asterisk
	${assert_fn}(req);
#end if
#else
int
${e.pre}req_${e.id}(struct galv_rpc_clnt *client,
                    ${e.pre}clt_${e.id}_fn *cb,
                    void * ctx)
{
#end if
	${assert_fn}(client);

	struct galv_rpc_clnt_msg *msg;
	struct hed_rpc_clnt_msg  *hed_msg;
#if 'request' in $e
	int                       ret;
#end if

	msg = galv_rpc_clnt_create_request(client,
	                                   ${e.pid.upper()},
	                                   ${e.pre}clt_dispatch_${e.id});
	if (!msg)
		return -ENOMEM;

	hed_msg = (struct hed_rpc_clnt_msg *)msg;
	hed_msg->u.async.cb = cb;
	hed_msg->u.async.ctx = ctx;
#if 'request' in $e

	ret = ${e.request.encode}(galv_rpc_clnt_msg_encoder(msg), req);
	if (ret)
		goto drop;
#end if

	return galv_rpc_clnt_push_msg(msg);
#if 'request' in $e

drop:
	galv_rpc_clnt_msg_drop(msg);
	return ret;
#end if
}

#set $goto = 'fini' if 'response' in e and 'fini' in $e.response and $e.response.fini else 'error'
int
${e.pre}clt_dispatch_${e.id}(struct galv_rpc_clnt_msg *msg, int event)
{
	${assert_fn}(msg);
	${assert_fn}(msg->id == ${e.pid.upper()});
	${assert_fn}(galv_rpc_clnt_msg_type(msg) == GALV_SESS_HEAD_REPLY_TYPE);

#if 'response' in $e
	${e.response.type} param;
#end if
	uint32_t status;
	int ret;
	struct dpack_decoder *decoder = galv_rpc_clnt_msg_decoder(msg);
	struct hed_rpc_clnt_msg *hed_msg = (struct hed_rpc_clnt_msg *)msg;
	${e.pre}clt_${e.id}_fn *cb = hed_msg->u.async.cb;
	void *ctx = hed_msg->u.async.ctx;

	${assert_fn}(cb);

	if (event) {
		${assert_fn}(event < 0);
		ret = event;
		goto error;
	}

	ret = dpack_decode_uint32(decoder, &status);
	if (ret)
		goto error;
#if 'response' in $e
#if 'init' in $e.response and $e.response.init

	ret = ${e.response.init}(&param);
	if (ret)
		goto error;
#end if

	if (!status) {
		ret = ${e.response.decode}(decoder, &param);
		if (ret)
			goto ${goto};
		ret = cb(ctx, status, ${e.response.ampersand}param);
	} else
#if $e.response.ampersand
		ret = cb(ctx, status, NULL);
#else
		ret = cb(ctx, status, 0);
#end if
#else
	ret = cb(ctx, status);
#end if
#if 'response' in $e and 'fini' in $e.response and $e.response.fini
	${e.response.fini}(&param);
#end if
	galv_rpc_clnt_msg_drop(msg);
	return 0;

#if 'response' in $e and 'fini' in $e.response and $e.response.fini
fini:
	${e.response.fini}(&param);
#end if
error:
	galv_rpc_clnt_msg_drop(msg);
#if 'response' in $e
#if $e.response.ampersand
	return cb(ctx, (uint32_t)-ret, NULL);
#else
	return cb(ctx, (uint32_t)-ret, 0);
#end if
#else
	return cb(ctx, (uint32_t)-ret);
#end if
}

static int
${e.pre}rsync_dispatch_${e.id}(struct galv_rpc_clnt_msg *msg, int event)
{
	${assert_fn}(msg);
	${assert_fn}(msg->id == ${e.pid.upper()});
	${assert_fn}(galv_rpc_clnt_msg_type(msg) == GALV_SESS_HEAD_REPLY_TYPE);

	int ret;
	struct dpack_decoder *decoder = galv_rpc_clnt_msg_decoder(msg);
	struct hed_rpc_clnt_msg *hed_msg = (struct hed_rpc_clnt_msg *)msg;

	if (event) {
		${assert_fn}(event < 0);
		*hed_msg->u.sync.status = (uint32_t)-event;
		goto drop;
	}

	ret = dpack_decode_uint32(decoder, hed_msg->u.sync.status);
#if 'response' in $e
	if (ret) {
		*hed_msg->u.sync.status = (uint32_t)-ret;
		goto drop;
	}

	${assert_fn}(hed_msg->u.sync.response);
	if (!*hed_msg->u.sync.status) {
		ret = ${e.response.decode}(decoder, hed_msg->u.sync.response);
		if (ret)
			*hed_msg->u.sync.status = (uint32_t)-ret;
	}
#else
	if (ret)
		*hed_msg->u.sync.status = (uint32_t)-ret;
#end if

drop:
	galv_rpc_clnt_msg_drop(msg);
	return 0;
}

#if 'request' in $e and 'response' in $e
extern int
${e.pre}rsync_${e.id}(struct galv_rpc_clnt *client,
                    ${e.request.type} ${e.request.asterisk}request,
                    ${e.response.type} *response)
#elif 'request' in $e
extern int
${e.pre}rsync_${e.id}(struct galv_rpc_clnt *client,
                    ${e.request.type} ${e.request.asterisk}request)
#elif 'response' in $e
extern int
${e.pre}rsync_${e.id}(struct galv_rpc_clnt *client,
                    ${e.response.type} *response)
#else
extern int
${e.pre}rsync_${e.id}(struct galv_rpc_clnt *client)
#end if
{
	${assert_fn}(client);
#if 'request' in $e and $e.request.asterisk
	${assert_fn}(request);
#end if
#if 'response' in $e
	${assert_fn}(response);
#end if

	struct galv_rpc_clnt_msg *msg;
	struct hed_rpc_clnt_msg  *hed_msg;
	int                       ret;
	uint32_t                  status;

	msg = galv_rpc_clnt_create_request(client,
	                                   ${e.pid.upper()},
	                                   ${e.pre}rsync_dispatch_${e.id});
	if (!msg)
		return -ENOMEM;

	hed_msg = (struct hed_rpc_clnt_msg *)msg;
	hed_msg->u.sync.status = &status;
#if 'response' in $e
	hed_msg->u.sync.response = response;
#end if
#if 'request' in $e

	ret = ${e.request.encode}(galv_rpc_clnt_msg_encoder(msg), request);
	if (ret)
		goto drop;
#end if

	ret = galv_rpc_clnt_push_msg(msg);
	if (ret)
		return ret;

	// TODO sync galv
	return (int)status;
#if 'request' in $e

drop:
	galv_rpc_clnt_msg_drop(msg);
	return ret;
#end if
}
#end for
#for $e in $rpc.evnt
#if 'request' in $e

int
${e.pre}ntf_${e.id}(struct galv_rpc_msg *conn,
                        ${e.request.type} ${e.request.asterisk}req)
{
#if $e.request.asterisk
	${assert_fn}(req);
#end if
#else
int
${e.pre}ntf_${e.id}(struct galv_rpc_msg *conn)
{
#end if
	${assert_fn}(conn);

	struct galv_rpc_msg   *msg;
	int                    ret = 0;

	msg = galv_rpc_create_notif(conn, ${e.pid.upper()});
	if (!msg)
		return -ENOMEM;
#if 'request' in $e

	ret = ${e.request.encode}(galv_rpc_msg_encoder(msg), req);
	if (ret)
		goto drop;
#end if

	return galv_rpc_push_msg(msg);
drop:
	galv_rpc_msg_drop(msg);
	return ret;
}

#set $goto = 'fini' if 'request' in e and 'fini' in $e.request else 'error'
int
${e.pre}clt_dispatch_${e.id}(struct galv_rpc_msg *msg)
{
	${assert_fn}(msg);
	${assert_fn}(msg->id == ${e.pid.upper()});
	${assert_fn}(galv_rpc_msg_get_type(msg) == GALV_SESS_HEAD_NOTIF_TYPE);

	struct galv_rpc_conn *conn = hed_msg_get_conn(msg);
#if 'request' in $e
	${e.request.type} param;
	int ret;
	struct dpack_decoder *decoder = galv_rpc_msg_decoder(msg);
#if 'init' in $e.request

	ret = ${e.request.init}(&param);
	if (ret)
		goto error;
#end if

	ret = ${e.request.decode}(decoder, &param);
	if (ret)
		goto ${goto};

	ret = ${e.pre}clt_${e.id}(conn, ${e.request.ampersand}param);
#if 'fini' in $e.request
fini:
	${e.request.fini}(&param);
#end if
error:
	galv_rpc_msg_drop(msg);
	return ret;
#else

	galv_rpc_msg_drop(msg);
	return ${e.pre}clt_${e.id}(conn);
#end if
}
#end for

#if 0
static const struct hed_rpc_auth ${pre}clt_auth[] = {
#for $e in $rpc.req + $rpc.evnt
#for $r in $e.roles
	{${e.pid.upper()}, ${r.elem.pid.upper()}, &${e.pre}clt_dispatch_${e.id}},
#end for
#end for
};

const struct galv_rpc_connect_conf
${pre}clt_conf = galv_rpc_connECT_CONF(0,
                                     SOCK_CLOEXEC,
                                     ${pre.upper()}CLT_MAX_PAYLOAD,
                                     CONFIG_HED_BUFF_CAPA_MAX
                                     ${rpc.maxid},
                                     stroll_array_nr(${pre}clt_auth),
                                     &${pre}clt_auth);
#end if
## ex: filetype=c
